# Google Drive Integration
# Required: Get this from Google Cloud Console > APIs & Services > Credentials > OAuth 2.0 Client IDs
# Setup steps:
#   1. Create a Google Cloud project (or use existing)
#   2. Enable the Google Drive API
#   3. Create OAuth 2.0 Client ID (Web application type)
#   4. Add your app URLs to Authorized JavaScript origins (e.g., http://localhost:5173 for dev)
#   5. Copy the Client ID below
VITE_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
