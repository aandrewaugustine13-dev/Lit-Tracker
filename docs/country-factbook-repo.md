# Country Factbook repository location

`Country Factbook` is maintained as a standalone repository (not in `Lit-Tracker`).

Local repository path:

- `/workspace/country-factbook`

This repository intentionally keeps `Country Factbook` out of the `Lit-Tracker` tree.
